{
  "name": "drupal-celebrations-project",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "workspaces": [
    "web/modules/custom/*",
    "web/themes/custom/*"
  ],
  "dependencies": {
    "platformsh-config": "^2.3.1"
  }
}

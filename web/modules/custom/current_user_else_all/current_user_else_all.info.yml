name: Current User else All
description: Add a Views Filter that uses the current user else all if current user is admin role.
package: Custom
type: module
core_version_requirement: ^9.0 || ^10.0 || ^11.0
